## Project Setup

```
npm install
node index.js
```

## Environment Variable

Create a .env file

    ```
        DB_HOST=localhost
        DB_USERNAME=root
        DB_PASSWORD=
        DB_NAME=products
    ```

## Directory

Create a uploads folder to store images
